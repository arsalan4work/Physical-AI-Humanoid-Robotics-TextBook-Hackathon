import React from 'react';
import TextSelectionHandler from '../TextSelectionHandler';

const Root = ({ children }) => {
  return <TextSelectionHandler>{children}</TextSelectionHandler>;
};

export default Root;